#ifndef SETTINGS_H
#define SETTINGS_H

const float mapSizeX  = 20;
const float mapSizeZ = 20;
const float bikeRadius = .5;
const float wallRadius = .1;
const float defaultBikeSpeed = 7;       // units per second
const float wallShrinkSpeed = 3;        // units per second
const float physicsTickTime = 1.0 / 60; // seconds per tick
const float viewHeight = 4;
const int bikesNum = 5;

#endif // SETTINGS_H

