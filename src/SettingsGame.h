/* SettingsGame.h
 * Controls several values related to the game.
 */
#ifndef SETTINGS_GAME_H
#define SETTINGS_GAME_H

const float mapSizeX  = 30;
const float mapSizeZ = 30;
const float bikeRadius = .2;
const float wallRadius = .07;
const float defaultBikeSpeed = 7;       // units per second
const float wallShrinkSpeed = 2;        // units per second
const float physicsTickTime = 1.0 / 60; // seconds per tick

#endif // SETTINGS_GAME_H

